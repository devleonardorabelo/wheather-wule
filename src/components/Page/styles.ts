import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  scrollView: {
    padding: 24,
    minHeight: '100%',
  },
  content: {
    flexGrow: 1,
  },
});

export default styles;
